# 🚀 دليل نشر بوت التداول الآلي

## 📌 الطريقة 1: التشغيل المحلي (على جهازك)

### الخطوات:
1. **افتح PowerShell أو CMD**
2. **انتقل للمجلد:**
   ```
   cd "C:\Users\lenovo\Desktop\بوت"
   ```
3. **شغّل البوت:**
   ```
   python web_app.py
   ```
   أو استخدم الملف الجاهز:
   ```
   run_web.bat
   ```
4. **افتح المتصفح:** http://127.0.0.1:5000

---

## 🌐 الطريقة 2: النشر على الإنترنت

### أ) النشر المجاني على PythonAnywhere

#### الخطوات:
1. **سجّل حساب مجاني:** https://www.pythonanywhere.com/registration/register/beginner/
2. **ارفع الملفات:**
   - اذهب إلى Files
   - ارفع جميع ملفات البوت (.py و requirements.txt)
3. **ثبّت المكتبات:**
   - افتح Bash Console
   - نفذ: `pip install --user -r requirements.txt`
4. **أنشئ Web App:**
   - اذهب إلى Web
   - اضغط Add a new web app
   - اختر Flask
   - حدد ملف web_app.py
5. **شغّل الموقع!**
   - سيعطيك رابط مثل: `username.pythonanywhere.com`

---

### ب) النشر على Render (مجاني)

#### الخطوات:
1. **ارفع المشروع لـ GitHub:**
   - أنشئ repository جديد
   - ارفع جميع الملفات
2. **سجّل في Render:** https://render.com/
3. **أنشئ Web Service:**
   - اضغط New > Web Service
   - اربط GitHub repository
   - اختر:
     - Build Command: `pip install -r requirements.txt`
     - Start Command: `python web_app.py`
4. **انشر!**
   - سيعطيك رابط مثل: `yourapp.onrender.com`

---

### ج) النشر على Heroku

#### الخطوات:
1. **سجّل حساب:** https://signup.heroku.com/
2. **ثبّت Heroku CLI:** https://devcenter.heroku.com/articles/heroku-cli
3. **أنشئ ملفات إضافية:**
   - `Procfile` (سننشئه لك)
   - `runtime.txt` (سننشئه لك)
4. **انشر عبر Git:**
   ```bash
   heroku login
   heroku create bot-name
   git push heroku main
   ```

---

## 📦 الطريقة 3: مشاركة البوت مع الأصدقاء

### كملف تنفيذي (.exe):
1. **ثبّت PyInstaller:**
   ```
   pip install pyinstaller
   ```
2. **أنشئ ملف تنفيذي:**
   ```
   pyinstaller --onefile --add-data "templates;templates" web_app.py
   ```
3. **شارك المجلد `dist/`** - يحتوي على web_app.exe

---

## ⚠️ ملاحظات مهمة:

### للنشر الحقيقي:
- ✅ غيّر `SIMULATION_MODE = False` في `config.py`
- ✅ أضف API keys حقيقية لمنصة التداول
- ✅ استخدم قاعدة بيانات حقيقية (SQLite أو PostgreSQL)
- ✅ أضف SSL للأمان (HTTPS)
- ⚠️ **لا تنشر مفاتيح API على GitHub!**

### الأمان:
- استخدم `.env` لحفظ المفاتيح السرية
- فعّل المصادقة (Login/Password)
- استخدم rate limiting

---

## 🎯 الخيار الموصى به للمبتدئين:

### **PythonAnywhere** - لأنه:
- ✅ مجاني
- ✅ سهل جداً
- ✅ يدعم Flask مباشرة
- ✅ لا يحتاج معرفة تقنية عميقة

---

## 📞 هل تحتاج مساعدة إضافية؟

أخبرني أي طريقة نشر تفضل وسأساعدك خطوة بخطوة! 🚀
