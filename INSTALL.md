# خطوات التثبيت والتشغيل

## 📋 المتطلبات
- Windows 10 أو أحدث
- Python 3.8 أو أحدث
- اتصال بالإنترنت

## 🔧 خطوات التثبيت

### 1. تحميل وتثبيت Python
إذا لم يكن Python مثبتاً:
1. اذهب إلى: https://www.python.org/downloads/
2. حمّل أحدث إصدار
3. عند التثبيت، تأكد من تفعيل "Add Python to PATH"

### 2. التحقق من تثبيت Python
افتح PowerShell وأدخل:
```powershell
python --version
```
يجب أن ترى رقم الإصدار (مثل: Python 3.11.0)

### 3. الانتقال لمجلد المشروع
```powershell
cd "C:\Users\lenovo\Desktop\بوت"
```

### 4. تثبيت المكتبات المطلوبة
```powershell
pip install -r requirements.txt
```

**ملاحظة**: قد يستغرق هذا بضع دقائق

### 5. التحقق من التثبيت
```powershell
python -c "import yfinance, pandas, ta; print('✅ جميع المكتبات مثبتة بنجاح!')"
```

## 🚀 التشغيل

### الطريقة الأولى: استخدام start.py (الأسهل)
```powershell
python start.py
```
ستظهر لك قائمة بسيطة لاختيار ما تريد تشغيله.

### الطريقة الثانية: تشغيل مباشر

#### تشغيل البوت الكامل
```powershell
python bot.py
```

#### اختبار التحليل التقني
```powershell
python technical_analysis.py
```

#### اختبار الاستراتيجيات
```powershell
python trading_strategy.py
```

#### اختبار إدارة المخاطر
```powershell
python risk_management.py
```

#### تشغيل الأمثلة
```powershell
python examples.py
```

## ⚙️ الإعدادات الأولية

### 1. تعديل قائمة الأسهم
افتح `config.py` وعدل:
```python
WATCHLIST = [
    "AAPL",  # Apple
    "MSFT",  # Microsoft
    "GOOGL", # Google
    # أضف أسهمك المفضلة هنا
]
```

### 2. تعديل إعدادات المخاطرة (اختياري)
في نفس الملف:
```python
MAX_POSITION_SIZE = 0.1      # نسبة رأس المال لكل صفقة
STOP_LOSS_PERCENT = 0.02     # نسبة وقف الخسارة
MAX_DAILY_LOSS = 0.05        # أقصى خسارة يومية
```

## 🧪 الاختبار الأولي

### اختبار سريع (موصى به للمبتدئين)
```powershell
# 1. اختبار التحليل
python technical_analysis.py

# 2. اختبار الاستراتيجيات
python trading_strategy.py

# 3. اختبار إدارة المخاطر
python risk_management.py

# 4. تشغيل البوت (اختر الخيار 1 - فحص واحد)
python bot.py
```

## 🐛 حل المشاكل الشائعة

### المشكلة: "python is not recognized"
**الحل**: 
1. أعد تثبيت Python وتأكد من تفعيل "Add to PATH"
2. أو أعد تشغيل PowerShell
3. أو استخدم المسار الكامل: `C:\Python311\python.exe`

### المشكلة: "pip is not recognized"
**الحل**:
```powershell
python -m pip install -r requirements.txt
```

### المشكلة: "ModuleNotFoundError: No module named 'xxx'"
**الحل**:
```powershell
pip install اسم_المكتبة
# أو
pip install -r requirements.txt --force-reinstall
```

### المشكلة: بطء التحميل
**الحل**: استخدم مرآة محلية:
```powershell
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### المشكلة: خطأ في تشغيل البوت
**الحل**:
1. تأكد من اتصالك بالإنترنت
2. تأكد من صحة رموز الأسهم في WATCHLIST
3. راجع رسالة الخطأ وابحث عنها

### المشكلة: "لا توجد بيانات للسهم"
**الحل**:
- للأسهم الأمريكية: استخدم الرمز مباشرة (AAPL)
- للأسهم السعودية: أضف .SR (2222.SR)
- تأكد من صحة الرمز

## 📚 التعلم والتطوير

### للمبتدئين
1. اقرأ `QUICKSTART.md` للبدء السريع
2. جرب `examples.py` للأمثلة العملية
3. اقرأ `GUIDE.md` للفهم المتعمق

### للمتقدمين
1. راجع الكود المصدري لفهم كيفية العمل
2. خصص الاستراتيجيات حسب أسلوبك
3. أضف ميزات جديدة

## ⚠️ تحذيرات مهمة

1. **ابدأ بوضع PAPER**: لا تستخدم التداول الحقيقي قبل اختبار شامل
2. **لا تترك البوت دون مراقبة**: خاصة في الأيام الأولى
3. **ابدأ برأس مال صغير**: عند التداول الحقيقي
4. **التزم بخطة المخاطرة**: لا تغير الإعدادات عاطفياً
5. **تعلم أولاً**: افهم كيف يعمل قبل الاستخدام الفعلي

## ✅ قائمة التحقق

قبل البدء، تأكد من:
- [ ] Python مثبت ويعمل
- [ ] جميع المكتبات مثبتة
- [ ] عدلت قائمة WATCHLIST
- [ ] اختبرت المكونات الفردية
- [ ] فهمت كيفية عمل البوت
- [ ] قرأت تحذيرات المخاطر
- [ ] جاهز للبدء!

## 🎯 الخطوات التالية

بعد التثبيت والاختبار:
1. شغل البوت في وضع Paper لأسبوع
2. راقب الإشارات والتوصيات
3. قارن بأدائك اليدوي
4. عدل الإعدادات حسب الحاجة
5. عندما تثق بالبوت، فكر في التداول الحقيقي

## 📞 الدعم

إذا واجهت مشاكل:
1. راجع هذا الدليل
2. اقرأ GUIDE.md للمعلومات التفصيلية
3. ابحث عن رسالة الخطأ على Google
4. افتح Issue في المستودع

---

**نتمنى لك تداولاً موفقاً! 🚀📈**
