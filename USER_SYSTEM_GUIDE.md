# 🔐 دليل نظام تسجيل الدخول وإشعارات واتساب

## ✅ ما تم إضافته

### 1. 🔐 نظام تسجيل دخول كامل
- ✅ صفحة تسجيل دخول احترافية
- ✅ صفحة إنشاء حساب جديد  
- ✅ قاعدة بيانات SQLite لحفظ المستخدمين
- ✅ تشفير كلمات المرور (PBKDF2 + SHA256)
- ✅ نظام جلسات آمن (Session Management)
- ✅ كل مستخدم له رصيد ومحفظة مستقلة

### 2. 📱 نظام إشعارات واتساب
- ✅ إشعار عند الشراء (رمز السهم، الكمية، السعر)
- ✅ إشعار عند البيع (الربح/الخسارة)
- ✅ إشعار عند تحقيق هدف الربح
- ✅ إشعار عند وقف الخسارة
- ✅ ملخص يومي للصفقات
- ✅ إشعار انخفاض الرصيد
- ✅ رسالة ترحيب للمستخدمين الجدد

---

## 📁 الملفات الجديدة

### 1. `user_system.py` (350 سطر)
**الغرض:** إدارة المستخدمين وقاعدة البيانات

**الجداول في قاعدة البيانات:**
```sql
- users: معلومات المستخدمين (اسم، بريد، رقم واتساب، رصيد)
- sessions: جلسات تسجيل الدخول
- transactions: سجل المعاملات المالية
- trades: سجل الصفقات (شراء/بيع)
```

**الوظائف الرئيسية:**
- `create_user()` - إنشاء مستخدم جديد
- `login_user()` - تسجيل دخول
- `verify_session()` - التحقق من الجلسة
- `update_balance()` - تحديث الرصيد
- `record_trade()` - تسجيل صفقة
- `close_trade()` - إغلاق صفقة وحساب الربح

### 2. `whatsapp_notifications.py` (250 سطر)
**الغرض:** إرسال إشعارات واتساب

**طرق الإرسال المدعومة:**
1. **Green API** (الأسهل - موصى به)
2. **Twilio WhatsApp API**
3. **Console Mode** (للتطوير)

**الوظائف الرئيسية:**
- `notify_buy()` - إشعار شراء
- `notify_sell()` - إشعار بيع
- `notify_profit_target()` - تحقيق هدف الربح
- `notify_stop_loss()` - وقف الخسارة
- `notify_daily_summary()` - ملخص يومي
- `notify_welcome()` - رسالة ترحيب

### 3. `login_pages.py` (600 سطر)
**الغرض:** صفحات HTML لتسجيل الدخول والتسجيل

**الصفحات:**
- `LOGIN_PAGE` - صفحة تسجيل الدخول
- `REGISTER_PAGE` - صفحة إنشاء حساب

### 4. تحديثات على `web_app.py`
**الإضافات:**
- استيراد `user_db` و `whatsapp_notifier`
- مفتاح سري للجلسات (`secret_key`)
- 6 routes جديدة:
  * `/login` - صفحة تسجيل الدخول
  * `/register` - صفحة التسجيل
  * `/dashboard` - لوحة التحكم (بعد تسجيل الدخول)
  * `/api/register` - API إنشاء حساب
  * `/api/login` - API تسجيل دخول
  * `/api/logout` - API تسجيل خروج

---

## 🚀 كيفية الاستخدام

### الخطوة 1: تشغيل التطبيق
```bash
cd "c:\Users\lenovo\Desktop\بوت"
python web_app.py
```

### الخطوة 2: إنشاء حساب جديد

1. افتح المتصفح: `http://127.0.0.1:5000`
2. سيتم تحويلك تلقائياً لصفحة **تسجيل الدخول**
3. اضغط على **"إنشاء حساب جديد"**
4. املأ البيانات:
   ```
   الاسم الكامل: أحمد محمد
   اسم المستخدم: ahmed123
   البريد: ahmed@example.com
   كلمة المرور: ••••••
   رقم واتساب: 966501234567
   ```
5. اضغط **"إنشاء الحساب"**
6. ستحصل على رسالة ترحيب على واتساب! 📱

### الخطوة 3: تسجيل الدخول

1. أدخل اسم المستخدم وكلمة المرور
2. اضغط **"تسجيل الدخول"**
3. سيتم تحويلك للوحة التحكم

### الخطوة 4: تفعيل إشعارات واتساب

#### أ) طريقة Green API (موصى بها - الأسهل)

1. سجل في **Green API**: https://green-api.com
2. احصل على:
   - Instance ID
   - API Token
3. افتح `whatsapp_notifications.py`
4. ضع بياناتك:
   ```python
   self.instance_id = "1101123456"  # ضع instance_id الخاص بك
   self.api_token = "abc123xyz..."   # ضع api_token الخاص بك
   ```
5. في الكود، غيّر طريقة الإرسال:
   ```python
   whatsapp_notifier.send_notification(phone, message, method='greenapi')
   ```

#### ب) طريقة Twilio WhatsApp API

1. سجل في **Twilio**: https://twilio.com
2. احصل على:
   - Account SID
   - Auth Token
   - WhatsApp Number
3. نصّب المكتبة:
   ```bash
   pip install twilio
   ```
4. افتح `whatsapp_notifications.py`
5. ضع بياناتك:
   ```python
   self.twilio_account_sid = "ACxxxx..."
   self.twilio_auth_token = "your_token"
   self.twilio_whatsapp_number = "whatsapp:+14155238886"
   ```
6. في الكود، غيّر طريقة الإرسال:
   ```python
   whatsapp_notifier.send_notification(phone, message, method='twilio')
   ```

#### ج) طريقة Console (للتطوير)

- لا تحتاج أي إعدادات
- الرسائل تُطبع في Terminal
- مفيدة للاختبار قبل ربط API حقيقي

---

## 📱 أمثلة الإشعارات

### 1. إشعار الشراء
```
🟢 *إشعار شراء* 🟢

📊 السهم: AAPL
📈 الكمية: 10 سهم
💵 السعر: $150.00
💰 التكلفة الإجمالية: $1,500.00

⏰ التاريخ: 2024-11-07 14:30:00

✅ تم تنفيذ الصفقة بنجاح!
```

### 2. إشعار البيع (ربح)
```
🟢 *إشعار بيع* 🟢

📊 السهم: AAPL
📉 الكمية: 10 سهم
💵 سعر الشراء: $150.00
💵 سعر البيع: $165.00
💰 ربح: $150.00

⏰ التاريخ: 2024-11-08 10:15:00

✅ تهانينا! صفقة ناجحة!
```

### 3. إشعار البيع (خسارة)
```
🔴 *إشعار بيع* 🔴

📊 السهم: TSLA
📉 الكمية: 5 سهم
💵 سعر الشراء: $250.00
💵 سعر البيع: $240.00
📉 خسارة: $50.00

⏰ التاريخ: 2024-11-08 11:00:00

⚠️ تم إغلاق الصفقة
```

### 4. وقف الخسارة
```
🛑 *تفعيل وقف الخسارة* 🛑

📊 السهم: NVDA
📉 الخسارة: $80.00
📊 النسبة: 5.0%

⏰ التاريخ: 2024-11-08 12:30:00

⚠️ تم إغلاق الصفقة تلقائياً لحماية رأس المال
```

### 5. الملخص اليومي
```
📊 *ملخص اليوم* 📊

📈 عدد الصفقات: 8
✅ صفقات رابحة: 6
❌ صفقات خاسرة: 2
📊 معدل النجاح: 75.0%

💰 الربح/الخسارة الإجمالي: $350.00

⏰ التاريخ: 2024-11-08

🎉 يوم رائع!
```

---

## 🔧 دمج إشعارات واتساب مع البوت

### مثال 1: إضافة إشعار عند شراء سهم

```python
# في ملف bot.py أو web_app.py
from whatsapp_notifications import whatsapp_notifier
from user_system import user_db

def buy_stock(user_id, symbol, quantity, price):
    """شراء سهم مع إرسال إشعار واتساب"""
    
    # تنفيذ عملية الشراء
    total_cost = quantity * price
    
    # تسجيل الصفقة في قاعدة البيانات
    trade_id = user_db.record_trade(user_id, symbol, 'buy', quantity, price)
    
    # الحصول على رقم واتساب المستخدم
    user_info = user_db.get_user_info(user_id)
    whatsapp_number = user_info['whatsapp_number']
    
    # إرسال إشعار واتساب
    if user_info['whatsapp_notifications']:  # إذا كانت الإشعارات مفعلة
        whatsapp_notifier.notify_buy(
            whatsapp_number,
            symbol,
            quantity,
            price,
            total_cost
        )
    
    return trade_id
```

### مثال 2: إضافة إشعار عند بيع سهم

```python
def sell_stock(user_id, trade_id, exit_price):
    """بيع سهم مع إرسال إشعار واتساب"""
    
    # الحصول على معلومات الصفقة
    conn = sqlite3.connect('trading_bot.db')
    cursor = conn.cursor()
    cursor.execute('SELECT symbol, quantity, entry_price FROM trades WHERE id = ?', (trade_id,))
    trade = cursor.fetchone()
    conn.close()
    
    symbol, quantity, entry_price = trade
    
    # حساب الربح/الخسارة
    profit = (exit_price - entry_price) * quantity
    
    # إغلاق الصفقة
    user_db.close_trade(trade_id, exit_price)
    
    # الحصول على رقم واتساب المستخدم
    user_info = user_db.get_user_info(user_id)
    whatsapp_number = user_info['whatsapp_number']
    
    # إرسال إشعار واتساب
    if user_info['whatsapp_notifications']:
        whatsapp_notifier.notify_sell(
            whatsapp_number,
            symbol,
            quantity,
            entry_price,
            exit_price,
            profit
        )
    
    return profit
```

---

## 🧪 اختبار النظام

### 1. اختبار إنشاء حساب
```bash
python -c "from user_system import user_db; print(user_db.create_user('test123', 'test@test.com', 'password123', 'Test User', '0501234567', '966501234567'))"
```

### 2. اختبار تسجيل الدخول
```bash
python -c "from user_system import user_db; print(user_db.login_user('test123', 'password123'))"
```

### 3. اختبار إشعار واتساب
```bash
python -c "from whatsapp_notifications import whatsapp_notifier; whatsapp_notifier.notify_buy('966501234567', 'AAPL', 10, 150.00, 1500.00)"
```

**النتيجة:** سترى الرسالة مطبوعة في Terminal (في وضع Console)

---

## 📊 قاعدة البيانات

### الموقع
```
c:\Users\lenovo\Desktop\بوت\trading_bot.db
```

### عرض البيانات
```bash
# تنصيب SQLite Browser (اختياري)
# أو استخدام Python:

python -c "
import sqlite3
conn = sqlite3.connect('trading_bot.db')
cursor = conn.cursor()
cursor.execute('SELECT * FROM users')
print(cursor.fetchall())
conn.close()
"
```

---

## 🔒 الأمان

### ما تم تطبيقه:
✅ تشفير كلمات المرور (PBKDF2-HMAC-SHA256)
✅ نظام جلسات آمن
✅ Session tokens عشوائية
✅ انتهاء صلاحية الجلسات (7 أيام)
✅ التحقق من صلاحية الجلسة في كل طلب

### توصيات:
⚠️ لا تشارك session tokens
⚠️ استخدم HTTPS في الإنتاج
⚠️ احفظ قاعدة البيانات في مكان آمن
⚠️ قم بنسخ احتياطي منتظم

---

## 🎨 تخصيص الإشعارات

يمكنك تعديل نص الإشعارات في `whatsapp_notifications.py`:

```python
def notify_buy(self, phone, symbol, quantity, price, total_cost):
    message = f"""
🟢 *إشعار شراء* 🟢

أضف الإيموجي والنص الذي تريد هنا!
"""
    return self.send_notification(phone, message.strip())
```

---

## 📞 الدعم

### للمساعدة في:
1. **إعداد Green API**: https://green-api.com/docs
2. **إعداد Twilio**: https://www.twilio.com/docs/whatsapp
3. **SQLite**: https://sqlite.org/docs.html

---

## ✅ الخلاصة

الآن لديك:
- 🔐 نظام تسجيل دخول كامل
- 👤 كل عميل له حساب مستقل
- 💰 محفظة منفصلة لكل مستخدم
- 📱 إشعارات واتساب تلقائية
- 📊 تتبع كامل للصفقات
- 🔒 أمان عالي

**🎉 كل شيء جاهز للاستخدام!**
